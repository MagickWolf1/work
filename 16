# list.append(x) - Добавляет элемент в конец списка
# list.extend(L) - Расширяет список list, добавляя в конец все элементы списка L
# list.insert(i, x) - Вставляет на i-ый элемент значение x
# list.remove(x) - Удаляет первый элемент в списке, имеющий значение x. ValueError, если такого элемента не существует
# list.pop([i]) - Удаляет i-ый элемент и возвращает его. Если индекс не указан, удаляется последний элемент
# list.index(x, [start [, end]]) - Возвращает положение первого элемента со значением x (при этом поиск ведется от start до end)
# list.count(x) - Возвращает количество элементов со значением x
# list.sort([key=функция], [reverse=False]) - Сортирует список на основе функции
# list.reverse() - Разворачивает список
# list.copy() - Возвращает копию списка
# list.clear() - Очищает список

l = [1, 2, 3, 'hello', ['test', 10], 'world', True]
name = ['ivanov','petrov', 'sidorov'] # иванов 0, петров 1 и т.д

#print([name[1]])
#print (l[4][0])
#print(l[0:3]) #несколько сразу символов то есть от 1 до 3
#l[:2] = [10,15]

l.append('new') # добавит новый элемент
l.extend([5,7])
l2 = ['hi', 19]
l += l2
l.insert(1,'testy') # двигает все элементы и стивит себя на то место куда хотим
#l.remove('world') # удаляет элемент
#el = l.pop() # удаляет позицию


#l.count('world') # считает элементы
#l.sort() #строку с числом не считает, только либо строки либо числа

print(l) #el пишем в принт

